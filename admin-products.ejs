<!DOCTYPE html>
<html dir="rtl" lang="ar">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> | وسيط DZ</title>
  <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body {
      font-family: 'Cairo', sans-serif;
      background: #0f172a;
      color: #e2e8f0;
    }
    .admin-container {
      display: flex;
      min-height: 100vh;
    }
    .sidebar {
      width: 280px;
      background: rgba(15,23,42,0.95);
      border-left: 1px solid #1e293b;
      padding: 2rem 1.5rem;
    }
    .sidebar h2 {
      background: linear-gradient(135deg, #60a5fa, #c084fc);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      font-size: 1.8rem;
      margin-bottom: 2rem;
    }
    .sidebar ul { list-style: none; }
    .sidebar li { margin-bottom: 0.5rem; }
    .sidebar a {
      display: flex;
      align-items: center;
      gap: 0.75rem;
      padding: 0.75rem 1rem;
      text-decoration: none;
      color: #94a3b8;
      border-radius: 12px;
      transition: all 0.3s;
    }
    .sidebar a:hover, .sidebar a.active {
      background: linear-gradient(135deg, #3b82f6, #2563eb);
      color: white;
    }
    .main-content {
      flex: 1;
      padding: 2rem;
    }
    .products-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
      gap: 1.5rem;
      margin-top: 1.5rem;
    }
    .product-card {
      background: rgba(30,41,59,0.8);
      backdrop-filter: blur(10px);
      border: 1px solid rgba(255,255,255,0.1);
      border-radius: 16px;
      padding: 1.5rem;
    }
    .product-header {
      display: flex;
      justify-content: space-between;
      align-items: start;
      margin-bottom: 1rem;
    }
    .product-title {
      font-size: 1.2rem;
      font-weight: 700;
      color: white;
    }
    .product-price {
      font-size: 1.3rem;
      font-weight: 800;
      color: #60a5fa;
    }
    .badge {
      padding: 0.25rem 0.75rem;
      border-radius: 20px;
      font-size: 0.8rem;
      font-weight: 600;
    }
    .badge-success { background: rgba(16,185,129,0.2); color: #a7f3d0; }
    .badge-warning { background: rgba(245,158,11,0.2); color: #fcd34d; }
    .badge-danger { background: rgba(239,68,68,0.2); color: #fecaca; }
    .btn {
      padding: 0.5rem 1rem;
      border: none;
      border-radius: 10px;
      cursor: pointer;
      text-decoration: none;
      display: inline-block;
      font-size: 0.9rem;
      margin: 0.25rem;
    }
    .btn-primary {
      background: linear-gradient(135deg, #3b82f6, #2563eb);
      color: white;
    }
    .btn-danger {
      background: linear-gradient(135deg, #ef4444, #dc2626);
      color: white;
    }
    .btn-sm {
      padding: 0.25rem 0.75rem;
      font-size: 0.8rem;
    }
    .alert {
      padding: 1rem;
      border-radius: 12px;
      margin-bottom: 1rem;
    }
    .alert-success {
      background: rgba(16,185,129,0.2);
      border-right: 4px solid #10b981;
      color: #a7f3d0;
    }
    .header-bar {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 2rem;
    }
  </style>
</head>
<body>
  <div class="admin-container">
    <div class="sidebar">
      <h2>وسيط DZ</h2>
      <ul>
        <li><a href="/admin"><i class="fas fa-tachometer-alt"></i> لوحة التحكم</a></li>
        <li><a href="/admin/products" class="active"><i class="fas fa-gamepad"></i> المنتجات</a></li>
        <li><a href="/admin/orders"><i class="fas fa-shopping-cart"></i> الطلبات</a></li>
        <li><a href="/admin/logout"><i class="fas fa-sign-out-alt"></i> تسجيل الخروج</a></li>
      </ul>
    </div>
    
    <div class="main-content">
      <% if (success_msg) { %>
        <div class="alert alert-success">
          <i class="fas fa-check-circle"></i> <%= success_msg %>
        </div>
      <% } %>
      
      <div class="header-bar">
        <h1 style="color: white;">إدارة المنتجات</h1>
        <a href="/admin/products/new" class="btn btn-primary"><i class="fas fa-plus"></i> إضافة منتج جديد</a>
      </div>
      
      <div class="products-grid">
        <% products.forEach(product => { %>
          <div class="product-card">
            <div class="product-header">
              <div>
                <h3 class="product-title"><%= product.name %></h3>
                <span style="color: #94a3b8; font-size: 0.9rem;">
                  <i class="far fa-calendar"></i> <%= new Date(product.createdAt).toLocaleDateString('ar-DZ') %>
                </span>
              </div>
              <span class="product-price"><%= product.price.toLocaleString() %> دج</span>
            </div>
            
            <div style="margin-bottom: 1rem;">
              <% if (product.status === 'available') { %>
                <span class="badge badge-success">متاح</span>
              <% } else if (product.status === 'pending') { %>
                <span class="badge badge-warning">قيد الانتظار</span>
              <% } else { %>
                <span class="badge badge-danger">تم البيع</span>
              <% } %>
              
              <% if (product.featured) { %>
                <span class="badge" style="background: rgba(245,158,11,0.2); color: #fcd34d; margin-right: 0.5rem;">
                  <i class="fas fa-crown"></i> مميز
                </span>
              <% } %>
            </div>
            
            <p style="color: #94a3b8; margin-bottom: 1rem; font-size: 0.9rem;">
              <%= product.description?.substring(0, 100) %>...
            </p>
            
            <div style="display: flex; gap: 0.5rem;">
              <a href="/admin/products/<%= product._id %>/edit" class="btn btn-primary btn-sm">
                <i class="fas fa-edit"></i> تعديل
              </a>
              <form action="/admin/products/<%= product._id %>/delete" method="POST" style="display: inline;">
                <button type="submit" class="btn btn-danger btn-sm" onclick="return confirm('هل أنت متأكد من الحذف؟')">
                  <i class="fas fa-trash"></i> حذف
                </button>
              </form>
            </div>
          </div>
        <% }) %>
      </div>
    </div>
  </div>
</body>
</html>
