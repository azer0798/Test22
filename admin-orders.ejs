<!DOCTYPE html>
<html dir="rtl" lang="ar">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> | وسيط DZ</title>
  <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body {
      font-family: 'Cairo', sans-serif;
      background: #0f172a;
      color: #e2e8f0;
    }
    .admin-container {
      display: flex;
      min-height: 100vh;
    }
    .sidebar {
      width: 280px;
      background: rgba(15,23,42,0.95);
      border-left: 1px solid #1e293b;
      padding: 2rem 1.5rem;
      position: fixed;
      height: 100vh;
    }
    .sidebar h2 {
      background: linear-gradient(135deg, #60a5fa, #c084fc);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      font-size: 1.8rem;
      margin-bottom: 2rem;
    }
    .sidebar ul { list-style: none; }
    .sidebar li { margin-bottom: 0.5rem; }
    .sidebar a {
      display: flex;
      align-items: center;
      gap: 0.75rem;
      padding: 0.75rem 1rem;
      text-decoration: none;
      color: #94a3b8;
      border-radius: 12px;
      transition: all 0.3s;
    }
    .sidebar a:hover, .sidebar a.active {
      background: linear-gradient(135deg, #3b82f6, #2563eb);
      color: white;
    }
    .main-content {
      flex: 1;
      margin-right: 280px;
      padding: 2rem;
    }
    .header-bar {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 2rem;
      background: rgba(30,41,59,0.5);
      padding: 1rem 1.5rem;
      border-radius: 16px;
      border: 1px solid rgba(255,255,255,0.1);
    }
    .stats-mini {
      display: flex;
      gap: 1.5rem;
      color: #94a3b8;
    }
    .stats-mini span {
      color: white;
      font-weight: 700;
      margin-left: 0.25rem;
    }
    .orders-table-container {
      background: rgba(30,41,59,0.8);
      backdrop-filter: blur(10px);
      border: 1px solid rgba(255,255,255,0.1);
      border-radius: 20px;
      padding: 1.5rem;
      overflow-x: auto;
    }
    table {
      width: 100%;
      border-collapse: collapse;
    }
    th {
      text-align: right;
      padding: 1rem;
      color: #94a3b8;
      font-weight: 600;
      border-bottom: 2px solid rgba(255,255,255,0.1);
    }
    td {
      padding: 1rem;
      border-bottom: 1px solid rgba(255,255,255,0.05);
    }
    tr:hover td {
      background: rgba(255,255,255,0.02);
    }
    .badge {
      padding: 0.25rem 0.75rem;
      border-radius: 20px;
      font-size: 0.8rem;
      font-weight: 600;
      display: inline-block;
    }
    .badge-pending { background: rgba(245,158,11,0.2); color: #fcd34d; border: 1px solid rgba(245,158,11,0.3); }
    .badge-completed { background: rgba(16,185,129,0.2); color: #a7f3d0; border: 1px solid rgba(16,185,129,0.3); }
    .badge-cancelled { background: rgba(239,68,68,0.2); color: #fecaca; border: 1px solid rgba(239,68,68,0.3); }
    .badge-contacted { background: rgba(96,165,250,0.2); color: #bfdbfe; border: 1px solid rgba(96,165,250,0.3); }
    .btn {
      padding: 0.4rem 0.8rem;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      text-decoration: none;
      display: inline-block;
      font-size: 0.8rem;
      transition: all 0.3s;
    }
    .btn-success {
      background: rgba(16,185,129,0.2);
      color: #a7f3d0;
      border: 1px solid rgba(16,185,129,0.3);
    }
    .btn-success:hover {
      background: rgba(16,185,129,0.3);
    }
    .btn-warning {
      background: rgba(245,158,11,0.2);
      color: #fcd34d;
      border: 1px solid rgba(245,158,11,0.3);
    }
    .btn-danger {
      background: rgba(239,68,68,0.2);
      color: #fecaca;
      border: 1px solid rgba(239,68,68,0.3);
    }
    .btn-info {
      background: rgba(96,165,250,0.2);
      color: #bfdbfe;
      border: 1px solid rgba(96,165,250,0.3);
    }
    .contact-buttons {
      display: flex;
      gap: 0.5rem;
    }
    .alert {
      padding: 1rem;
      border-radius: 12px;
      margin-bottom: 1.5rem;
    }
    .alert-success {
      background: rgba(16,185,129,0.2);
      border-right: 4px solid #10b981;
      color: #a7f3d0;
    }
    .filter-bar {
      display: flex;
      gap: 1rem;
      margin-bottom: 1.5rem;
      flex-wrap: wrap;
    }
    .filter-btn {
      padding: 0.5rem 1rem;
      background: rgba(255,255,255,0.05);
      border: 1px solid rgba(255,255,255,0.1);
      border-radius: 30px;
      color: #94a3b8;
      text-decoration: none;
      transition: all 0.3s;
    }
    .filter-btn.active {
      background: #3b82f6;
      color: white;
      border-color: #3b82f6;
    }
    .filter-btn:hover {
      background: rgba(59,130,246,0.2);
      color: white;
    }
  </style>
</head>
<body>
  <div class="admin-container">
    <div class="sidebar">
      <h2>وسيط DZ</h2>
      <ul>
        <li><a href="/admin"><i class="fas fa-tachometer-alt"></i> لوحة التحكم</a></li>
        <li><a href="/admin/products"><i class="fas fa-gamepad"></i> المنتجات</a></li>
        <li><a href="/admin/orders" class="active"><i class="fas fa-shopping-cart"></i> الطلبات</a></li>
        <li><a href="/admin/logout"><i class="fas fa-sign-out-alt"></i> تسجيل الخروج</a></li>
      </ul>
    </div>
    
    <div class="main-content">
      <% if (success_msg) { %>
        <div class="alert alert-success">
          <i class="fas fa-check-circle"></i> <%= success_msg %>
        </div>
      <% } %>
      
      <div class="header-bar">
        <div>
          <h1 style="color: white; margin-bottom: 0.5rem;">إدارة الطلبات</h1>
          <div class="stats-mini">
            <span><i class="fas fa-shopping-cart"></i> إجمالي: <span><%= orders.length %></span></span>
            <span><i class="fas fa-clock" style="color: #f59e0b;"></i> قيد الانتظار: <span><%= orders.filter(o => o.status === 'pending').length %></span></span>
            <span><i class="fas fa-check-circle" style="color: #10b981;"></i> مكتمل: <span><%= orders.filter(o => o.status === 'completed').length %></span></span>
            <span><i class="fas fa-phone" style="color: #60a5fa;"></i> تم التواصل: <span><%= orders.filter(o => o.status === 'contacted').length %></span></span>
          </div>
        </div>
      </div>
      
      <div class="filter-bar">
        <a href="/admin/orders" class="filter-btn <%= !filters || !filters.status ? 'active' : '' %>">
          <i class="fas fa-list"></i> الكل
        </a>
        <a href="/admin/orders?status=pending" class="filter-btn <%= filters && filters.status === 'pending' ? 'active' : '' %>">
          <i class="fas fa-clock"></i> قيد الانتظار
        </a>
        <a href="/admin/orders?status=contacted" class="filter-btn <%= filters && filters.status === 'contacted' ? 'active' : '' %>">
          <i class="fas fa-phone"></i> تم التواصل
        </a>
        <a href="/admin/orders?status=completed" class="filter-btn <%= filters && filters.status === 'completed' ? 'active' : '' %>">
          <i class="fas fa-check-circle"></i> مكتمل
        </a>
        <a href="/admin/orders?status=cancelled" class="filter-btn <%= filters && filters.status === 'cancelled' ? 'active' : '' %>">
          <i class="fas fa-times-circle"></i> ملغي
        </a>
      </div>
      
      <div class="orders-table-container">
        <table>
          <thead>
            <tr>
              <th>#</th>
              <th>المنتج</th>
              <th>العميل</th>
              <th>الهاتف</th>
              <th>المبلغ</th>
              <th>طريقة الدفع</th>
              <th>الحالة</th>
              <th>التاريخ</th>
              <th>الإجراءات</th>
            </tr>
          </thead>
          <tbody>
            <% orders.forEach((order, index) => { %>
              <tr>
                <td style="color: #60a5fa; font-weight: 600;">#<%= order._id.toString().slice(-6) %></td>
                <td>
                  <div style="display: flex; align-items: center; gap: 0.5rem;">
                    <img src="<%= order.product?.image || 'https://via.placeholder.com/40' %>" 
                         style="width: 40px; height: 40px; border-radius: 8px; object-fit: cover;">
                    <div>
                      <div style="color: white;"><%= order.product?.name || 'محذوف' %></div>
                      <small style="color: #94a3b8;"><%= order.product?.category || '-' %></small>
                    </div>
                  </div>
                </td>
                <td>
                  <div style="font-weight: 600; color: white;"><%= order.customerName %></div>
                  <small style="color: #94a3b8;"><%= order.customerEmail || 'لا يوجد بريد' %></small>
                </td>
                <td><%= order.customerPhone %></td>
                <td style="color: #60a5fa; font-weight: 700;"><%= order.product?.price?.toLocaleString() || '0' %> دج</td>
                <td>
                  <span style="color: #94a3b8;">
                    <% if (order.paymentMethod === 'ccp') { %>CCP<% } %>
                    <% if (order.paymentMethod === 'baridimob') { %>Baridi Mob<% } %>
                    <% if (order.paymentMethod === 'flexy') { %>Flexy<% } %>
                    <% if (order.paymentMethod === 'other') { %>أخرى<% } %>
                  </span>
                </td>
                <td>
                  <% if (order.status === 'pending') { %>
                    <span class="badge badge-pending"><i class="fas fa-clock"></i> قيد الانتظار</span>
                  <% } else if (order.status === 'contacted') { %>
                    <span class="badge badge-contacted"><i class="fas fa-phone"></i> تم التواصل</span>
                  <% } else if (order.status === 'completed') { %>
                    <span class="badge badge-completed"><i class="fas fa-check-circle"></i> مكتمل</span>
                  <% } else { %>
                    <span class="badge badge-cancelled"><i class="fas fa-times-circle"></i> ملغي</span>
                  <% } %>
                </td>
                <td style="color: #94a3b8; font-size: 0.9rem;">
                  <%= new Date(order.createdAt).toLocaleDateString('ar-DZ') %>
                  <br><small><%= new Date(order.createdAt).toLocaleTimeString('ar-DZ') %></small>
                </td>
                <td>
                  <div style="display: flex; flex-direction: column; gap: 0.5rem;">
                    <form action="/admin/orders/<%= order._id %>" method="POST" style="display: flex; gap: 0.25rem; flex-wrap: wrap;">
                      <button type="submit" name="status" value="contacted" class="btn btn-info btn-sm" 
                              <%= order.status === 'contacted' || order.status === 'completed' ? 'disabled' : '' %>>
                        <i class="fas fa-phone"></i>
                      </button>
                      <button type="submit" name="status" value="completed" class="btn btn-success btn-sm"
                              <%= order.status === 'completed' ? 'disabled' : '' %>>
                        <i class="fas fa-check"></i>
                      </button>
                      <button type="submit" name="status" value="cancelled" class="btn btn-danger btn-sm"
                              <%= order.status === 'cancelled' || order.status === 'completed' ? 'disabled' : '' %>>
                        <i class="fas fa-times"></i>
                      </button>
                    </form>
                    
                    <div class="contact-buttons">
                      <a href="/contact/whatsapp/<%= order._id %>" target="_blank" class="btn btn-success btn-sm" style="background: rgba(37,211,102,0.2); color: #d1fae5;">
                        <i class="fab fa-whatsapp"></i>
                      </a>
                      <a href="/contact/telegram/<%= order._id %>" target="_blank" class="btn btn-info btn-sm" style="background: rgba(0,136,204,0.2); color: #bfdbfe;">
                        <i class="fab fa-telegram"></i>
                      </a>
                    </div>
                  </div>
                </td>
              </tr>
            <% }) %>
          </tbody>
        </table>
        
        <% if (orders.length === 0) { %>
          <div style="text-align: center; padding: 3rem;">
            <i class="fas fa-shopping-cart" style="font-size: 3rem; color: #475569; margin-bottom: 1rem;"></i>
            <h3 style="color: white;">لا توجد طلبات</h3>
            <p style="color: #94a3b8;">لم يتم تقديم أي طلبات بعد</p>
          </div>
        <% } %>
      </div>
    </div>
  </div>
</body>
</html>
